<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kitaro.informationcentertistr">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainInnoFood" />
        <activity android:name=".MainInnoAg" />
        <activity android:name=".MainInnoEN" />
        <activity android:name=".MainInnoHerb" />
        <activity android:name=".MainInnoMat" />
        <activity android:name=".MainInnoRobot" />
        <activity android:name=".ServiceActivity"></activity>
    </application>

</manifest>